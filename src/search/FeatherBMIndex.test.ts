describe('FeatherBMIndex', () => {
    /*
    test('PartialBM Score R + S', () => {

        const index = new FeatherBMIndex("test", 1, 1);
        const score = index.partial_bm_score(1, 1, 1, 1, 1);
        expect(score).toBe(0.5);
    });*/

});